# UI / UX

## Locked constraints

- Light mode only
- Gold reflecting borders / chrome direction (polish in Phase 1+)
- Muse: ghost text, Tab accept, other key dismiss
- Archive → typed-name delete

## Surfaces

| Route | Role |
|-------|------|
| `/` | Project browser |
| `/project/:id` | Writing surface (preserve) |
| `/design` | **Phase 1 workbench** — live experiments |

Phase 0 = wire UI. Phase 1 iterates visual design with the human in `/design` before production chrome ships.