# Phase 4 — Notes, Journal, Blog

**Status: FULL STOP — awaiting Jeremy live verify** (continuous Phase 3→4 run; stop only here.)

Standing law: [`docs/MASTER_PLAN.md`](../MASTER_PLAN.md). Design: design-reference part-2 §§2–4 + IA Journal hierarchy.

## Steps

| Step | Name | Status |
|------|------|--------|
| 4.1 | Notes module (capture, auto-tag stubs, Ask-your-vault lexical search) | done (machine) |
| 4.2 | Blog stage strip + Set aside | done (machine) |
| 4.3 | Journal Books + Public/Private encryption + recovery key + Keychain | done (machine) |
| 4.4 | Journal writing surface (Zen default, dated tabs, 18px) | done (machine) |
| 4.5 | Journal map (opt-in MapLibre shell) — after core privacy/writing | done (shell) |
| 4.6 | Drawing tool popup shell (shared eight-tool kit UI) | done (shell) |
| 4.7 | Module wiring audit + FULL STOP for Jeremy (end of continuous run) | done (machine) |

## Sequencing note

Core Books/privacy/writing before map. Sidecar/iPad liability copy lives with drawing settings disclosure, not a full Sidecar stack.

## Out of scope

Phase 5 AI (NL Ask-your-vault agent, Blog AI generation, Journal memory follow-ups). Export/publish (Phase 8). Real MapLibre/PMTiles tiles. Real canvas paint (PENS / Phase 6).

## Machine notes / known roughness

- Notes `#Hashtag` → silent Codex Character stub (not full AI auto-organize). Ask-your-vault = lexical search in tray.
- Journal private books: Fernet seal/open via session DEK; recovery key one-time; keyring helper when available. Unlock UI in ModuleWorkspace.
- Journal Zen = ModuleWorkspace-local only (same DraftShell-only lesson — no StudioApp split state).
- Map = placeholder shell (no MapLibre/OSM/PMTiles wired yet).
- Drawing popup = eight-tool + two-input UI shell; Sidecar liability copy present.
- Blog: Research → Outline → Draft → Edit strip; Set aside badge (retrievable, never silent discard). AI review cards are non-blocking shells.
