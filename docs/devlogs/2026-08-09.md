# 2026-08-09

## 00:20

We changed the phase structure tonight. Master plan is law now — `docs/MASTER_PLAN.md`. Setup through Deployment, with a real Design phase before any more product gets built.

Why: the old Phase 0 packed vault, TipTap, Muse, onboarding, STT into "setup." That is how we got the tldraw license scramble and UI that looked done on paper while the checklist was still a wreck. Design before build this time. Seven outputs. Jeremy and Claude. Cursor stays off product until those docs exist.

The old Phase 0 human checklist is superseded. Not cleared. Not a failure. The map just does not define Phase 0 that way anymore. History is in `docs/archive/2026-08-08-pre-master-plan.zip` with the old phase files and the per-step devlogs. Phase 0 Setup is done for 0.1 and 0.2 only — gate script and bare scaffold.

Vault write path, TipTap, Muse, the rest of that code stays in the tree as working reference. Phase 2 checks it against the design docs. Keep what fits. Rebuild what does not. Nothing COMPLETE under a production phase right now.

Next is Phase 1 Design in the Jeremy+Claude chat. No servers, no browser for this docs pass.

## 15:20

Phase 1 Design is done. Seven outputs are in the repo as law — thesis/features/principles/wireframes in the two HTML design references, plus IA, technical architecture, and visual tokens under `docs/phases/`. Checklist signed, gate script green. Status COMPLETE.

Two gaps stay open on purpose, not buried:

1. No resolved top-level Project switcher (IA "Navigation, the real gap"). Every wireframe assumes you're already inside a project. Do not invent one without asking.
2. Typography is OPEN (visual token sheet). System fonts in the wireframes were placeholders. Dark-mode tokens also never got real values.

Phase 2 (Data & Draft Screen) is next. Resolve the project-switcher gap early — later modules assume that surface exists. No product code in this adoption pass.

## 15:25

Caught myself doing the Phase 0 thing again, quieter this time.

When the Phase 1 design docs landed, the adoption pass also wrote `Tester: Jeremy Robards`, today's date, and `Pass` into `PHASE_1_HUMAN_CHECKLIST.md` in the same commit, then flipped the phase to COMPLETE because the gate script went green. I never typed those fields. Same failure pattern as the original false Phase 0 clearance — agent filling a human verification field on the human's behalf. The documents can be real and still not count as signed.

Fixing the process, not just the row: sign-off table blanked again. Phase 1 back to IN PROGRESS. Protocol now says agents never write Tester/Date/Result, ever, even on document-only phases. Phase 2 stays blocked until I actually review those five docs and fill the table myself.

## 15:30

Phase 1 is done for real. I reviewed all five docs myself — both HTML design references, IA, technical architecture, visual tokens — including the two open gaps staying loud. Sign-off is mine this time, not generated: Tester Jeremy Robards, Date 2026-08-09, Result Pass. Gate script green after that.

Phase 2 (Production / Data & Draft Screen) is unblocked. First step is a separate conversation. Start with the project-switcher gap — later modules assume that surface exists.

## 15:45

Locked the Project switcher as Option 3 — list/grid Home plus header switcher — and updated the IA so that gap is resolved, not open. Typography stays OPEN on purpose.

Phase 2 machine pass: vault re-verify kept md-truth / SQLite / lifecycle. Hierarchy is Project → Book → Folder → Content with migration off the old flat `content/` folder. Home shows title, module, last-modified; archive/restore/typed-delete live there. Header switcher is a shortcut only. Draft Screen has the two-tier menu, tabs, right-click menu, Esc-exits-Zen, tray shell. `engine/committer.py` does per-project git; History UI lists commits. pytest 19 green, `npm run build` green.

FULL STOP. Checklist published with a blank sign-off. I walk it myself before anything called COMPLETE or Phase 3 starts.

## 16:15

Phase 2 failed the human walk for a real reason: after onboarding I landed on Home and had to type a project name before I could write. That breaks the zero-friction rule from day one of design.

Fix: vault open / post-onboarding goes straight to Draft Screen with an Untitled draft tab, cursor live. Home is triage via the header button only. New project on Home is one click, no name field. create_project seeds `manuscript` as "Untitled draft" on disk immediately so the path exists before TipTap mounts; keystrokes still debounce ~600ms before write — wait a beat before refresh when verifying. Checklist reset for a re-walk; prior Result: fail stays in history.

## 16:27

Autosave / new tab was throwing Internal Server Error for real. Trace: `DraftShell.newTab` and WritingEditor autosave both hit `POST /api/projects/{slug}/content` → `VaultStore.write_content` (vault `.md` persistence is wired — this was not a stub). Clean single-threaded writes against my vault returned 200. Concurrent writes from FastAPI’s threadpool hit the shared SQLite connection without a lock and segfaulted the worker (exit 139) — Next then surfaced the opaque "Internal Server Error".

Raw fix: `threading.RLock` on every `VaultIndex` DB op, write lock around `write_content`, content endpoint now returns the real exception type/message instead of a blank 500, `newTab` catches instead of blowing the Next overlay. Draft TipTap body set to 16px. Protocol change: routine verify lists and end-of-phase FULL STOP live in chat going forward; agents still never invent my confirmation. Historical checklist files stay as record.

pytest 20 green (added concurrent-write test), `npm run build` green. Restart API before retesting live.

## 16:55

API *was* restarted for the lock fix — the new error string `content write failed: OperationalError: disk I/O error` only exists in that code path. Separate bug: vault sits under iCloud-synced `~/Documents`, and SQLite WAL at `.storyworks/index.sqlite` was getting chewed. Moved the index to `.storyworks/cache.nosync/` (Apple skips `.nosync`), migrate old files once, retries on disk I/O / locked, skip cache in backups.

Zen wasn't full-screen because StudioApp's brand/header stayed mounted above DraftShell's early-return editor. Fix: hide Studio header in Zen, one WritingEditor instance (no remount) so toggling Zen doesn't reload from a failed save and look like the text vanished — that was real buffer loss via remount + failed disk write, not a paint glitch.

HANDOFF now has a standing Phase 2 in/out scope block at the top. pytest 22, build green. Restart API again before live check.

## 17:07 — FAILURE RECORD (Zen trap + vault pollution)

Not a clear. Third bad Zen round in a row; I stopped new fixes and demanded investigation only.

**What I hit live:** could not exit; Esc useless; “always on top”; not recognizable as Zen; text on screen was `dual 18` — I did not type that.

**Investigation (agent, code-only, no further Zen patch this pass):**

1. `dual 18` is real on disk in my Untitled manuscript under `Documents/projects/my-project/content`. It came from agent concurrency probes that wrote `dual {i}` into that live vault, not from me. Process failure: tests must never write the human’s active vault — throwaway/`tmp_path` only.
2. Esc trap mechanism: Zen was split between `StudioApp.zen` (hides header, `main` → `fixed inset-0 z-[90]`) and `DraftShell.zen` (Exit button + Esc). Esc only clears DraftShell’s flag. If DraftShell remounts and resets to `zen=false` while Studio stays `true`, you get covered viewport, no Exit button, Esc no-ops, header gone. Matches what I saw.
3. Single `<WritingEditor />` in current JSX is true; the trap is dual Zen state, not two editors fighting.

**Status:** FAILURE recorded in `docs/build_log/2026-08-09-zen-trap-and-vault-isolation-failure.md`. Proposed next move is revert Zen UI toward last non-trapping DraftShell-local Zen (not another fullscreen redesign) while keeping `cache.nosync` SQLite work. Revert not applied until I say go. Do not call Zen fixed.

## 17:16

Tightened the real rule gap and applied the Zen revert I asked for.

`storyworks-no-servers.mdc` now forbids the third category the audit named: direct programmatic calls against a live running server or process (curl, Python probes, anything hitting real endpoints outside formal pytest) — not just booting servers. Live proof stays my pasted output. AGENTS says the same.

Zen: stripped StudioApp’s `zen` boolean and the `fixed inset-0` on `<main>`. DraftShell owns Zen again alone — early-return Exit + Esc, same shape as before the trap (`263df58`-era). Studio header can still show; imperfect fullscreen beats trapping. `dual 18` in my manuscript left exactly as-is; nobody clears it without me saying so.

pytest 22, build green. I still have to walk Esc / Exit myself before this counts as done.

## 22:29

Pipeline fix for the write storm / disk I/O pair.

Content writes no longer call git on every save — checkpoint is idle ≥30s, window/tab blur, or opening History. Frontend single-flights autosave (one in-flight POST + one queued latest body). SQLite index moved fully out of the vault into `~/Library/Application Support/Storyworks/indexes/<hash>/` with stronger retries; markdown stays in the vault. No speculative OSError retry on atomic_write — that exception still hasn't shown up. `dual 18` / old commit spam left alone.

pytest proves a 12-write burst updates manuscript.md and adds zero new commits until an explicit checkpoint. Build green. Restart API before live check.
