# 0.1 — quality protocol landed

Audited the repo tonight and Phase 0’s thin slice was fine (health, Muse, archive/delete, gitignore) but the new step rules weren’t in the tree at all. No pytest, no `PHASE_STEP_PROTOCOL`, and three Mycelia Time docs sitting in `docs/reference/` that don’t belong here.

Deleted the capture-agent, check-in, and next-build-wave files. Kept `authentic-voice-notes.md` and pointed it at Storyworks. Added `docs/PHASE_STEP_PROTOCOL.md`, `storyworks-quality-gate.mdc`, swapped the old phase0-stop rule for `storyworks-phase-current.mdc`, and wired AGENTS / INSTRUCTIONS / HANDOFF so the 100% gate and phase FULL STOP aren’t optional.

Also made `STORYWORKS_DATA_DIR` / `STORYWORKS_PROJECTS_DIR` overridable so the next step can run API tests without touching my real projects folder. `npm run build` still green.

Phase 0 is not “done done” yet. Need the automated suite and the human checklist before anyone touches `/design`.
