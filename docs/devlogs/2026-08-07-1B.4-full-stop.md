# 1B.4 — Docs FULL STOP

Rewrote the Phase 1 docs for the redesign that actually shipped, not the old tab sandbox.

`PHASE_1.md` now tracks 1B.1–1B.4. Human checklist is click paths for full-bleed shell, playlist/opacity, solid modules, aides, R3F drawer, mobile, and the writing-path preserve checks. HANDOFF, `06_UI_UX.md`, and `docs/design/README.md` match the new law. Rejected tab chrome is called out so nobody polishes the corpse.

Stopping here. No Phase 2 until I clear `PHASE_1_HUMAN_CHECKLIST.md` myself. Still only have the paper placeholder in the playlist — real stills can land in `assets/backgrounds/` whenever without changing the protocol.
