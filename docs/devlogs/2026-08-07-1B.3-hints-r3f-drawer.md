# 1B.3 — Hover aides everywhere + R3F drawer

Went through the chrome and labeled the damn thing. Every region has a visible aide and a `title` hover: brand, module switcher (with id micro-labels), opacity, today’s asset, the solid panel, Muse line, footer status, drawer controls.

Drawer is the first real R3F use — slides in from the right with a simple spinning card mesh. Daily BG stays webp. Escape and scrim close it. Three.js balloons the bundle (chunk warning on build); fine for a sandbox, can code-split later if it bothers anyone.

`pytest` still 6/6. `npm run build` clean aside from the size note. Docs and human checklist next, then FULL STOP.
