# 0.2 — pytest for the thin slice

Had zero tests before this step, which made the new 100% gate kind of a joke. Added `pytest` plus an isolated API suite under `tests/` that spins temp `STORYWORKS_DATA_DIR` / `STORYWORKS_PROJECTS_DIR` so it doesn’t chew on real manuscripts.

Six tests, all green: health, connector shapes, create/write/archive/restore/typed-delete + backup folder, delete-blocked-when-live, mocked Muse shape, gitignore guard. `npm run build` still fine.

Feels better knowing archive-before-delete isn’t just vibes anymore.
