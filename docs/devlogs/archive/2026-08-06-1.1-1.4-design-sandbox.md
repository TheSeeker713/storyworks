# 1.1–1.4 — `/design` finally looks like a studio

Cleared Phase 0 and went straight into the sandbox. Stub is gone. Fraunces for the brand line, Manrope for UI, gold edge shadow instead of the dashed construction tape.

Tabs for tokens, three project browser layouts, three editor surfaces, two Muse chromes, a working delete-modal mock, and a short IA note on where Codex and Writers Room should sit later. Production `/` and the real editor are untouched on purpose.

`pytest` still 6/6 and `npm run build` clean. Stopping for my own click-through on `PHASE_1_HUMAN_CHECKLIST.md` before anyone ports this chrome into production routes.
