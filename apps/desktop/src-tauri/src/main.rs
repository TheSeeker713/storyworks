// Scaffold only. Full `tauri::Builder` + `generate_context!` needs icons,
// capabilities, and a completed `cargo tauri init` on Jeremy's machine.
// See apps/desktop/README.md and docs/phases/DEPLOYMENT.md.

fn main() {
    eprintln!(
        "storyworks-desktop scaffold: packaging incomplete until Tauri init + Apple signing (Jeremy)."
    );
}
